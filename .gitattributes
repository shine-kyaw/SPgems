* text=auto eol=lf

# PowerShell 5.1 reads BOM-less .ps1 files as ANSI and mangles them into parse
# errors, so these must keep their UTF-8 BOM and CRLF endings.
*.ps1 text eol=crlf working-tree-encoding=UTF-8

*.png binary
*.jpg binary
*.webp binary
*.avif binary
*.woff2 binary
*.mp4 binary
